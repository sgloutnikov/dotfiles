# Homebrew
export HOMEBREW_NO_INSTALL_CLEANUP=1

# Python (Homebrew)
export PATH="$(brew --prefix)/opt/python@3.13/libexec/bin:$PATH"
eval "$(uv generate-shell-completion zsh)"

# LM Studio CLI (lms)
export PATH="$PATH:/Users/sgloutnikov/.lmstudio/bin"
